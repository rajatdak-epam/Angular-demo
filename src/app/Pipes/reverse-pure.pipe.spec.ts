import { ReversePurePipe } from './reverse-pure.pipe';

describe('ReversePurePipe', () => {
  it('create an instance', () => {
    const pipe = new ReversePurePipe();
    expect(pipe).toBeTruthy();
  });
});
