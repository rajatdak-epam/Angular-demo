import { SortedImpurePipe } from './sorted-impure.pipe';

describe('SortedImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new SortedImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
